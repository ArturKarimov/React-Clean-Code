import React from 'react';

const About = () => {
    return (
            <h1>
                In this app you can see main concept React with React Hooks
            </h1>
    );
};

export default About;